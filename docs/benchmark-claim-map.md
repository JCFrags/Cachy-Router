# Claim Map

Use this map before turning cache-router experiments into README text, release
notes, blog posts, or benchmark tables.

## Claim Tiers

| tier | meaning |
|---|---|
| Portable behavior | Expected from the code path itself and covered by offline checks. |
| Exploratory live result | Measured in a bounded trusted-LAN experiment; useful, but not production proof. |
| Architecture plan | Designed and documented, but not fully implemented or validated. |
| Future work | Planned but not yet tested. |
| Not claimed | Must not be presented as working behavior. |

## Current Public Claims

| claim | tier | acceptance status | acceptable wording | evidence |
|---|---|---|---|---|
| The router exposes an OpenAI-compatible surface for normal completion and chat pass-through. | Portable behavior | `done` offline route smoke; deployment-wide worker-matrix success remains `live-gated` | "Cachy Router provides OpenAI-style `/v1/completions`, `/v1/chat/completions`, and `/v1/models` endpoints for trusted-LAN deployments." | `scripts/cache_router_daemon.py`, `scripts/cache_router_daemon_smoke_test.py`, `scripts/cache_router_live_endpoint_matrix.py`, `docs/architecture/cache-router-openai-endpoint.md`, `docs/architecture/final-acceptance-metrics.md` |
| Worker inventories are not limited to two machines. | Portable behavior | `done` | "The inventory format accepts one row per worker; two workers are only an example." | `configs/cache-router/workers.example.json`, `scripts/cache_router_make_inventory.py`, `scripts/cache_router_setup_doctor_matrix_test.py`, `docs/architecture/final-acceptance-metrics.md` |
| Normal non-cached streaming passes through for completion and chat requests. | Portable behavior | `done` for normal non-cached and bypass requests; cached build/use streaming is rejected | "Normal non-cached `stream: true` completion and chat requests are relayed through the router; `cache_router.mode=bypass` may stream after extension stripping, while cached build/use streaming is rejected clearly." | `scripts/cache_router_daemon.py`, `scripts/cache_router_daemon_smoke_test.py`, `docs/architecture/cache-router-openai-endpoint.md`, `docs/architecture/final-acceptance-metrics.md` |
| Normal proxy fallback can survive an offline worker disconnect. | Portable behavior | `done` for offline loopback backend-disconnect fallback only | "In offline smoke coverage, a normal completion request whose first worker disconnects mid-request falls back to a remaining ready worker, records a bounded `worker_unavailable` decision event, and the next request is served by the remaining ready worker." | `scripts/cache_router_daemon.py`, `scripts/cache_router_daemon_smoke_test.py`, `docs/architecture/final-acceptance-metrics.md` |
| The explicit cache extension can build, restore, and use prefix caches through suffix-only routing. | Scoped live result | `done` for one no-MTP trusted-LAN suffix benchmark gate; correctness gates remain separate | "A reviewed no-MTP live gate showed large prompt-processing reduction when a restored prefix was followed by a suffix-only route." | `evidence/cache-router-results-summary.md`, `scripts/cache_router_suffix_benchmark_gate.py`, `docs/architecture/final-acceptance-metrics.md` |
| Repeatable suffix-route benchmark thresholds are operator-runnable. | Scoped live result | `done` for one no-MTP trusted-LAN deployment; MTP and distributed correctness proof remain separate | "Cachy Router includes an operator-run suffix benchmark gate for fixed-probe prompt-eval token reduction and true TTFT. A no-MTP live run passed the 10-run gate with zero fallback, zero restore errors, and no full-reprocess evidence." | `scripts/cache_router_suffix_benchmark_gate.py`, `evidence/cache-router-results-summary.md`, `docs/architecture/final-acceptance-metrics.md`, `docs/benchmark-claim-map.md` |
| Restore correctness gates are operator-runnable, with one no-MTP deployment pass. | Scoped live result | scoped `done` for one no-MTP trusted-LAN gate; MTP and logits/top-k remain `live-gated` or `blocked-by-upstream` | "A no-MTP live gate passed 10 runs each for deterministic generation, system-prompt boundary, mid-conversation, after tool output, near-context long prefix, and branch/retry restore checks with router-side deterministic recompute validation and no fallback. This does not prove MTP-enabled restore correctness, logits/top-k equivalence, or arbitrary distributed-cache correctness." | `scripts/cache_router_correctness_probe.py`, `schemas/cache-router/cache-validation-result.schema.json`, `scripts/validate_cache_router_contracts.py`, `evidence/cache-router-results-summary.md`, `docs/architecture/final-acceptance-metrics.md` |
| Router-owned durable blobs plus worker-local hot copies are the intended cache ownership model. | Exploratory live result | `done` for offline store/sidecar mechanics; cross-worker live proof remains `live-gated` | "The prototype demonstrated router-owned blob storage and worker-local hydration for compatible workers." | `evidence/cache-router-results-summary.md`, `docs/architecture/cache-router.md`, `scripts/cache_router_store_audit.py`, `scripts/cache_router_transport.py`, `docs/architecture/final-acceptance-metrics.md` |
| The router may run on a worker or an independent aggregator machine. | Portable behavior | `done` documentation claim | "Run the router on any trusted-LAN host that can reach worker URLs and sidecars." | `README.md`, `docs/architecture/cache-router-setup.md`, `docs/architecture/final-acceptance-metrics.md` |
| The sidecar can reject eviction for a locally leased artifact. | Portable behavior | `done` for sidecar-local cooperative leases | "The sidecar can reject `/evict` for a locally leased artifact when cooperating clients hold an unexpired sidecar lease." | `scripts/cache_router_worker_sidecar.py`, `scripts/cache_router_sidecar_smoke_test.py`, `docs/architecture/final-acceptance-metrics.md` |
| The router store can reject duplicate cache-key operations with a persisted lease. | Portable behavior | `done` for local persisted registry leases under one shared cache root | "Cachy Router stores cooperative registry leases in `router-store/registry-leases.json` under a file lock and rejects duplicate same-cache-key build/upload or hydrate/restore work held by another router owner." | `scripts/cache_router_daemon.py`, `scripts/cache_router_daemon_smoke_test.py`, `scripts/cache_router_store_audit.py`, `docs/architecture/final-acceptance-metrics.md` |
| Tenant/scope cache policy can miss-mask denied cache lookups. | Portable behavior | `done` offline daemon smoke; trusted-LAN hashed-policy mitigation only | "Request-supplied hashed tenant/scope metadata scopes cache build and lookup; wrong-tenant use or wrong-conversation use of a conversation-scoped cache is denied before restore or generation, admin-audited, and client-masked as a scoped miss; `scope=tenant` is the explicit same-tenant broader reuse mode. Auth-derived tenant isolation is not claimed." | `scripts/cache_router_daemon.py`, `scripts/cache_router_daemon_smoke_test.py`, `docs/architecture/cache-router.md`, `docs/architecture/final-acceptance-metrics.md` |
| Durable blob encryption-at-rest has a documented operator audit path. | Portable behavior | `done` offline setup/store audit only | "Router-owned durable blobs are expected to live under an operator-managed encrypted cache root; inventories and manifests carry non-secret attestation metadata, setup doctor validates it, and store audit can reject active manifests without valid encryption-at-rest metadata. This is an operator storage audit path, not Python-level blob encryption." | `configs/cache-router/workers.example.json`, `scripts/cache_router_setup_doctor.py`, `scripts/cache_router_store_audit.py`, `schemas/cache-router/cache-manifest.schema.json`, `docs/architecture/final-acceptance-metrics.md` |
| Strict cache-key material constrains restore lookup. | Portable behavior | `done` for supplied-hash equality, request-prefix/runtime-derived lookup, and canonical selected-manifest self-check | "Cache use requires either an exact supplied `cache_router.cache_key_hash` or non-empty `prefix_text` that lets the daemon derive the request/runtime strict key. Cache-id-only restore is rejected before lookup, and selected manifests must recompute to their own canonical strict key before hydrate or restore." | `scripts/cache_router_daemon.py`, `scripts/cache_router_daemon_smoke_test.py`, `docs/architecture/cache-router-openai-endpoint.md`, `docs/architecture/final-acceptance-metrics.md` |
| Optional router-side queueing can affect normal proxy routing and reject overflow before backend forwarding. | Portable behavior | `done` for queue-depth routing, queue metrics, bounded rejection, and offline RSS-bound rejected-load proof | "When explicitly enabled, per-worker router queues contribute to normal proxy scheduling, and queue-full requests receive bounded OpenAI-shaped `503` errors before backend forwarding while the offline rejected-load smoke test keeps process RSS within the current 110% gate." | `scripts/cache_router_daemon.py`, `scripts/cache_router_daemon_smoke_test.py`, `docs/architecture/cache-router-openai-endpoint.md`, `docs/architecture/final-acceptance-metrics.md` |
| Target loopback concurrency reaches the configured active-slot count. | Portable behavior | `done` for offline loopback target concurrency only | "The offline performance probe runs two fake workers with two configured active slots each and verifies four simultaneous explicit-worker completion requests finish without client exceptions, record decision events, and drain active/queued counts afterward." | `scripts/cache_router_performance_probe.py`, `Makefile`, `docs/architecture/final-acceptance-metrics.md` |
| Ten-minute loopback scheduler stress remains stable. | Portable behavior | `done` for offline scheduler/accounting stress only | "The scheduler stress probe runs for 600 seconds at `2 * N * active_slots` against loopback fake workers and verifies no client exceptions, no invalid checked decision events, no active/queued leaks, and no traffic to an unavailable worker." | `scripts/cache_router_scheduler_stress_probe.py`, `docs/architecture/final-acceptance-metrics.md` |
| The 8-hour leak-check gate is executable. | Architecture plan | `partial`; harness exists, acceptance math is self-tested, full-duration result required | "The long-soak probe can run an 8-hour loopback router RSS gate with a one-hour baseline, 1% per-hour growth threshold, final RSS <=110% of baseline, zero hard failures, and zero bounded 429/503 responses; `make check` covers only the short harness smoke plus synthetic acceptance-math self-tests, not a full-duration soak result." | `scripts/cache_router_long_soak_probe.py`, `Makefile`, `docs/architecture/final-acceptance-metrics.md` |
| Local synthetic normal proxy overhead meets the current p95 gate. | Portable behavior | `done` for offline loopback fake-worker probes only | "In the offline loopback fake-worker benchmark, normal proxy overhead is checked against p95 <50 ms by subtracting direct fake-worker latency from routed latency." | `scripts/cache_router_performance_probe.py`, `Makefile`, `docs/architecture/final-acceptance-metrics.md` |
| Local registry cache lookup latency meets the current p95 gate. | Portable behavior | `done` for offline local-registry probes only | "In the offline local-registry benchmark, cache lookup plus manifest validation is checked against p95 <25 ms." | `scripts/cache_router_performance_probe.py`, `Makefile`, `docs/architecture/final-acceptance-metrics.md` |
| The project is ready for untrusted networks or public internet exposure. | Not claimed | `not-claimed` | Do not claim this. | Required before claiming: authentication, authorization, tenant isolation, rate limits, TLS, hardening, and adversarial testing. |
| Distributed cache correctness is proven for arbitrary models and runtimes. | Not claimed | `not-claimed`; relevant gates are `blocked-by-upstream` or `live-gated` | Do not claim this. | Required before claiming: strict-key negatives, logit/top-k or deterministic restore checks, model hash validation, backend compatibility checks, and repeated tests. |
| Cachy Router is production load-balancing infrastructure. | Not claimed | `not-claimed`; optional queueing, target loopback concurrency, ten-minute loopback scheduler stress, and loopback disconnect fallback are offline-tested, but long-soak, live production load, and operations gates remain `partial` or `live-gated` | Do not claim this. | Required before claiming: production scheduling policy, live stress tests, production retention/GC orchestration, durability policy, monitoring, and operational packaging. |

## Required Caveats

- Cache restore is only safe for compatible model, tokenizer, template, runtime,
  backend, KV type, context, and speculative-decoding settings.
- The accelerated path is explicit: build or restore a prefix, then send only
  the suffix/new work. Full-prompt replay may still reprocess the prompt.
- No-key mode is for trusted private LANs only.
- Raw proof artifacts should stay private unless they are redacted into a small
  public summary.
